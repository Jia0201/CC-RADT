---
id: "prompts-lead-retry-v1.0.0"
title: "Lead Retry Prompt v1.0.0"
type: "retry-prompt"
scope: "agent"
owner: "lead"
status: active
version: "1.0.0"
agent: "lead"
---
# Lead Retry Prompt v1.0.0

```xml
<retry_contract task_id="{{task_id}}" agent_id="lead" failed_attempt="{{failed_attempt}}">
  <error_evidence>{{error_evidence}}</error_evidence>
  <root_cause>{{root_cause}}</root_cause>
  <corrective_instruction>
    重新确认路由、任务契约、Agent 状态、锁、权限和安全门禁；只修正已确认的调度根因。
    {{corrective_instruction}}
  </corrective_instruction>
  <reduced_scope>{{reduced_scope}}</reduced_scope>
  <forbidden_actions>{{forbidden_actions}}</forbidden_actions>
  <verification>{{verification}}</verification>
  <handoff_target>{{handoff_target}}</handoff_target>
</retry_contract>
```

再次失败时停止重试，选择改派、拆分、降级、等待用户或关闭。
