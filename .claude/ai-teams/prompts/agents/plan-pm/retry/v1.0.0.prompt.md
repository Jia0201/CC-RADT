---
id: "prompts-plan-pm-retry-v1.0.0"
title: "Plan-PM Retry Prompt v1.0.0"
type: "retry-prompt"
scope: "agent"
owner: "lead"
status: active
version: "1.0.0"
agent: "plan-pm"
---
# Plan-PM Retry Prompt v1.0.0

```xml
<retry_contract task_id="{{task_id}}" agent_id="plan-pm" failed_attempt="{{failed_attempt}}">
  <error_evidence>{{error_evidence}}</error_evidence>
  <root_cause>{{root_cause}}</root_cause>
  <corrective_instruction>
    修正任务漏项、依赖、Owner、锁或 QA 节点；缩短方案，不重新扩展需求。
    {{corrective_instruction}}
  </corrective_instruction>
  <reduced_scope>{{reduced_scope}}</reduced_scope>
  <forbidden_actions>{{forbidden_actions}}</forbidden_actions>
  <verification>{{verification}}</verification>
  <handoff_target>{{handoff_target}}</handoff_target>
</retry_contract>
```

再次失败时把不可执行计划和阻塞证据交给 Lead。
