---
id: "prompts-dev-backend-service-retry-v1.0.0"
title: "Dev-Backend-Service Retry Prompt v1.0.0"
type: "retry-prompt"
scope: "agent"
owner: "lead"
status: active
version: "1.0.0"
agent: "dev-backend-service"
---
# Dev-Backend-Service Retry Prompt v1.0.0

```xml
<retry_contract task_id="{{task_id}}" agent_id="dev-backend-service" failed_attempt="{{failed_attempt}}">
  <error_evidence>{{error_evidence}}</error_evidence>
  <root_cause>{{root_cause}}</root_cause>
  <corrective_instruction>
    只修正已确认的 API、字段、迁移、配置、运行时或测试根因；保留契约和数据兼容。
    {{corrective_instruction}}
  </corrective_instruction>
  <reduced_scope>{{reduced_scope}}</reduced_scope>
  <forbidden_actions>{{forbidden_actions}}</forbidden_actions>
  <verification>{{verification}}</verification>
  <handoff_target>{{handoff_target}}</handoff_target>
</retry_contract>
```

不得硬编码密钥、吞掉错误、删除迁移证据或绕过接口验证。
