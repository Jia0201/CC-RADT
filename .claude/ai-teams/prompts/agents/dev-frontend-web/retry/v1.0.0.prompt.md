---
id: "prompts-dev-frontend-web-retry-v1.0.0"
title: "Dev-Frontend-Web Retry Prompt v1.0.0"
type: "retry-prompt"
scope: "agent"
owner: "lead"
status: active
version: "1.0.0"
agent: "dev-frontend-web"
---
# Dev-Frontend-Web Retry Prompt v1.0.0

```xml
<retry_contract task_id="{{task_id}}" agent_id="dev-frontend-web" failed_attempt="{{failed_attempt}}">
  <error_evidence>{{error_evidence}}</error_evidence>
  <root_cause>{{root_cause}}</root_cause>
  <corrective_instruction>
    只修正已确认的页面、字段、状态、构建或测试根因；重新核对 UI 与接口契约。
    {{corrective_instruction}}
  </corrective_instruction>
  <reduced_scope>{{reduced_scope}}</reduced_scope>
  <forbidden_actions>{{forbidden_actions}}</forbidden_actions>
  <verification>{{verification}}</verification>
  <handoff_target>{{handoff_target}}</handoff_target>
</retry_contract>
```

不得通过假数据、删测试、放宽类型或跳过错误态获得通过。
