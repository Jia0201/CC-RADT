---
id: "prompts-qa-retry-v1.0.0"
title: "QA Retry Prompt v1.0.0"
type: "retry-prompt"
scope: "agent"
owner: "lead"
status: active
version: "1.0.0"
agent: "qa"
---
# QA Retry Prompt v1.0.0

```xml
<retry_contract task_id="{{task_id}}" agent_id="qa" failed_attempt="{{failed_attempt}}">
  <error_evidence>{{error_evidence}}</error_evidence>
  <root_cause>{{root_cause}}</root_cause>
  <corrective_instruction>
    修正漏测、错误断言、环境判断或证据不足；只重跑受影响验证并补充回归面。
    {{corrective_instruction}}
  </corrective_instruction>
  <reduced_scope>{{reduced_scope}}</reduced_scope>
  <forbidden_actions>{{forbidden_actions}}</forbidden_actions>
  <verification>{{verification}}</verification>
  <handoff_target>{{handoff_target}}</handoff_target>
</retry_contract>
```

不得把未运行测试标为通过，也不得修改实现来迎合测试。
