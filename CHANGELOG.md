# CC-RADT 变更日志

本文件记录用户可感知的功能、规则、兼容性和安装变化。开发过程细节保留在 Git 提交与 `logs/`，正式发布证据由安装包根目录的 `RELEASE_RECORD.md`、manifest 和 checksum 共同提供。

格式参考 Keep a Changelog，版本号遵循 Semantic Versioning。

## [Unreleased]

暂无待归档变更。

## [1.2.0] - 2026-09-22

正式发布 Observer V2、升级清单与外部升级安全边界。构建来源、文件清单和校验值记录在发布附件及安装包中。

### Added

- 现有记录详情直接美化 Markdown：Agent / Skill / 工作流定义、日志证据及上一版本、公开对话和成功 Read 的 Markdown 文件统一排版标题、列表、表格与代码块；不增加文档入口，不改变只读文件范围，JSON 和修改差异保留原样。渲染依赖随包本地提供，HTML 转义并消毒，不自动加载图片或执行文档代码。
- 观察台接入当前项目 Claude Code 原生主 / 子会话记录，按用户任务查看公开对话、工具输入输出、Agent 协作与逐实例去重 Token 用量；不采集隐藏思考，不复制原始会话到安装包或观察器历史。
- 新增文件变更、Skills、MCP、软链路菜单；分别展示 CC 修改片段、项目 Git 工作区差异、实际技能文件、脱敏 MCP 配置和 playbook 声明的工作流。
- 概览改为按会话组织的 Agent 状态卡片，团队与配置改为项目级能力名册；证据与审查集中展示权限请求、人类提问、计划确认和任务变更。
- 追加只读 PermissionRequest / permission_prompt 观察 Hook。没有调用 ID 或审批人时保持未知，网页仍无执行或批准入口。
- 观察台采用紧凑的黑白灰界面，统一线性图标与轻量展开；自动刷新保留展开状态与键盘焦点，Esc 返回原记录，并尊重减少动态效果设置。
- 新增外部升级清单生成器与首版契约：使用逐文件 canonical SHA-256 标识构建，区分系统、项目数据、配置与生成材料；支持包外 sidecar，不改写已发布包。
- 新增实验阶段独立 Go 升级器：本机网页与 CLI 共用预览、原件保全、三方合并、逐文件切换与持久化恢复内核；支持固定 GitHub 仓库 ID 的 Release 下载。已完成 macOS 隔离验证，Windows 写入保持禁用，不能作为全平台稳定升级器发布。
- `claude-subdir` 正式包在运行内容定稿后生成 `upgrade-manifest.json`，进入最终 checksum；实验升级器独立交付，源码与二进制均不随 Harness 分发。
- 升级清单按已知角色、版本化 prompt 结构、精确内置 Skill 资产和 MCP/KB 初始化文件区分静态资产与运行数据，支持更新未改官方文件并阻断双边定制冲突；旧包 sidecar 依据已核对旧布局分类，不读取最新本地 registry 或改写旧包。

### Security

- 停用工程内旧升级脚本的 apply 和会写快照的 dry-run；help、错误参数及旧调用均不写运行目录，统一指向外部升级流程。
- 升级事务绑定目录对象身份，记录和事件原子发布且使用递增序号；保全本地 UID/GID，不继承发布机归属。并发新内容、未知迁移、权限冲突和中断状态均阻断或进入独立恢复，不调用 Agent 自升级。
- 未签名清单默认仅可预览；实际 apply 必须由用户明确核对并认可旧、新清单完整指纹，不将生成清单或 seal 当作官方签名。混合目录未知文件默认 data，生成索引本地与新版同时修改时保守阻断，不声明已实现智能图谱合并。

### Documentation

- README 前移并扩展研发观测台介绍，加入真实界面的概览、任务证据、审查与移动端截图，标明隔离测试样例。
- 增加版本分支导航：`v1.0.0` 保留旧版，`main` 展示新版与后续文档改进；已发布标签和附件保持不变。
- 增加外部升级安全规则、清单 Schema 与迁移声明边界；工程内升级指令改为只提供用户操作说明，不代表签名发布或跨平台升级验收已完成。
- 同步中英文源码与安装包 README、使用指南、发布说明和升级指南，补充 Observer V2 实际界面截图，保留旧版发布与迁移记录。

### Fixed

- E2E 夹具复制排除私有配置、凭据、缓存和独立升级器产物，避免带入隔离验收目录。
- 源码链接检查不再将 `updater/artifacts/` 中的历史材料当作当前文档。
- 安装包使用指南的观察台链接按 `.claude/ai-teams/` 布局生成，避免复制源码相对链接后出现断链。
- 正式包同时排除普通仓库的 `.git/` 目录和 worktree 的 `.git` 指针文件，避免泄露发布机路径或污染运行包。

## [1.1.0] - 2026-09-06

### Added

- 内置只读研发观察台：CC 会话首次启动提示本机访问地址，同项目多会话并发复用服务，按会话隔离活动与 Agent 标识；支持项目日志、任务 / 审查文件和角色定义的只读查看与版本留存，不从网页执行任务。
- 建立 Git 分支、提交、版本、发布记录和回滚治理体系。
- 为中英文 README 增加四张生成式流程与架构图片。
- 增加完整使用指南、v1.0.0 → v1.1.0 升级指南与中英文发布说明，随正式包分发。

### Changed

- Lead 主会话不再设置会抢占用户真实需求的 `initialPrompt`；默认 `agent: lead` 保留，入口状态由隐藏 Hook 上下文控制。
- Lead system prompt 升级到 `1.0.1`，强制每次具名 Agent 派发前使用任务提示词渲染器，避免手写合同遗漏验收字段。
- 多 Agent 心跳不再把 Lead 等待或仅有 30 秒 Hook 静默误判为接管失败；已完成 Agent 会清理旧告警，真实工具与权限失败仍立即接管。
- 敏感文件 Hook 改用精确文件名和目录判断，不再误拦截 `TokenUtils.java`、`TokenResult.java`、`AuthFilterToken.java` 等正常鉴权源码。
- 项目初始化新增 `project/.initialized` 运行态标记；自检区分待分发纯净包与已初始化运行态，初始化日志不再被误报为安装包污染。
- 正式包自检脚本改为按脚本位置解析 AI-Teams 根目录，可直接从目标项目根目录调用。
- 项目初始化与 Rule 刷新统一忽略 Maven/Gradle/前端等构建产物目录，避免生成文件污染项目画像和索引。
- 敏感文件权限改为项目根相对的精确文件模式，继续保护环境、凭据、密钥和 token 文件，同时不再误伤 `TokenUtils.java` 等正常源码。
- 项目初始化改为用户主动操作；Claude Code 启动、恢复会话和普通请求不再自动询问、自动执行或写入初始化画像，未初始化时 Git 增量 Hook 静默跳过。
- 主分支 README 采用开发分支的完整项目介绍，并补强安装、团队、工作流、记忆、安全、提示词、Skills、MCP 和文档导航。
- 正式包保留 ADR 状态目录和原子锁目录，Git 克隆后可直接通过结构自检。
- 源工程补充空 ADR 目录占位文件，避免干净克隆因缺少目录而误报 ADR 编号缺失。
- 发布来源记录的文件计数包含首次生成的记录自身，与最终 manifest 和实际文件数一致。
- 正式包内部 README 使用运行态说明，并排除源码二次开发指南及版本治理断链。

## [1.0.0] - 2026-08-09

### Added

- 首个公开安装版本。
- 12 个 Claude Code 具名 Agent、12 套工作流、四层记忆、项目初始化、规则路由、安全治理、Hooks、Skills、MCP 和提示词治理。
- `.claude/ai-teams/` 运行布局、项目级 Agent 与 Rules 适配层、安装自检和隔离 Fixture E2E。

[Unreleased]: https://github.com/Jia0201/CC-RADT/compare/v1.2.0...dev
[1.2.0]: https://github.com/Jia0201/CC-RADT/releases/tag/v1.2.0
[1.1.0]: https://github.com/Jia0201/CC-RADT/releases/tag/v1.1.0
[1.0.0]: https://github.com/Jia0201/CC-RADT/releases/tag/v1.0.0
