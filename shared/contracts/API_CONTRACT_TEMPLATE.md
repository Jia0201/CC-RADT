---
id: "shared-contracts-api-contract-template"
title: "API 接口契约模板"
type: "shared-template"
scope: "project"
owner: "doc"
status: active
---
# API 接口契约模板

> [!IMPORTANT]
> 创建契约时保留本模板结构，删除填写提示并补齐真实内容。字段、行为或验证未确定时，状态必须保持 `draft`。

## 1. 基本信息

| 项目 | 内容 |
|---|---|
| 契约名称 | `<业务动作或接口名称>` |
| 契约 ID | `<稳定唯一标识>` |
| 协议 | `HTTP / GraphQL / RPC / WebSocket / Event` |
| 方法与路径/主题 | `<例如 GET /v1/users/{userId}>` |
| 契约版本 | `<例如 1.0.0>` |
| 状态 | `draft / review / approved / deprecated / archived` |
| 接口 Owner | `<后端或平台负责人>` |
| 前端消费方 | `<Web / 小程序 / App / SDK 负责人>` |
| QA | `<验证负责人>` |
| Doc | `<文档维护负责人>` |
| 创建/更新时间 | `<YYYY-MM-DD>` |
| 替代/被替代契约 | `<标准 Markdown markdown-link 或无>` |

## 2. 目标与边界

- 业务目标：`<接口解决的问题>`
- 调用方：`<允许调用的客户端或服务>`
- 非目标：`<本接口明确不承担的能力>`
- 前置条件：`<资源、状态、权限或依赖要求>`

## 3. 事实源与关联

| 类型 | 链接 | 权威关系 |
|---|---|---|
| 机器可读 Schema | `<OpenAPI / GraphQL / Protobuf / JSON Schema 路径>` | `<文档生成 Schema / Schema 生成类型 / 人工同步>` |
| 后端实现 | `<文件或模块链接>` | `<实现必须符合本契约>` |
| 前端类型/映射 | `<文件或模块链接>` | `<消费类型必须符合本契约>` |
| Mock/夹具 | `<文件或服务链接>` | `<Mock 必须符合本契约>` |
| QA 用例 | `<测试或验证文档链接>` | `<作为门禁证据>` |
| 项目文档 | `[architecture](../../project/architecture.md)`、`[verification](../../project/verification.md)` | `<需要同步的章节>` |

## 4. 调用约束

| 项目 | 契约 |
|---|---|
| 鉴权 | `<方式、权限范围、Token/Cookie 位置>` |
| 幂等 | `<是否幂等；幂等键位置和有效期>` |
| 超时 | `<客户端/服务端超时>` |
| 重试 | `<可重试条件、退避、最大次数>` |
| 限流 | `<维度、配额、响应头或错误码>` |
| 缓存 | `<缓存键、TTL、失效条件>` |
| 内容类型 | `<例如 application/json>` |

## 5. 请求契约

### 5.1 Path 参数

| 字段 | 类型 | 必填 | 可空 | 格式/范围 | 默认值 | 示例 | 说明 |
|---|---|---|---|---|---|---|---|
| `<name>` | `<string>` | 是 | 否 | `<约束>` | 无 | `<value>` | `<语义>` |

### 5.2 Query 参数

| 字段 | 类型 | 必填 | 可空 | 格式/范围 | 默认值来源 | 示例 | 说明 |
|---|---|---|---|---|---|---|---|
| `<name>` | `<string>` | 否 | 否 | `<约束>` | `<服务端/客户端/无>` | `<value>` | `<语义>` |

### 5.3 Header

| 字段 | 类型 | 必填 | 格式/范围 | 示例 | 说明 |
|---|---|---|---|---|---|
| `<header-name>` | `<string>` | `<是/否>` | `<约束>` | `<value>` | `<语义>` |

### 5.4 Body

| 字段路径 | 类型 | 必填 | 可空 | 格式/范围/单位 | 默认值来源 | 示例 | 后端语义 | 前端来源/映射 |
|---|---|---|---|---|---|---|---|---|
| `<profile.name>` | `<string>` | 是 | 否 | `<1..100 字符>` | 无 | `<示例>` | `<存储/校验语义>` | `<表单字段或转换>` |

### 5.5 请求示例

```json
{
  "replace": "with a valid request example"
}
```

## 6. 成功响应契约

- 协议状态：`<例如 HTTP 200>`
- 业务成功条件：`<可机器判断的条件>`

| 字段路径 | 类型 | 必填 | 可空 | 格式/范围/单位 | 默认值来源 | 示例 | 后端语义 | 前端消费/映射 |
|---|---|---|---|---|---|---|---|---|
| `<data.id>` | `<string>` | 是 | 否 | `<ID 规则>` | 无 | `<id>` | `<资源标识>` | `<ViewModel.id>` |

### 6.1 响应示例

```json
{
  "replace": "with a valid response example"
}
```

## 7. 字段专项约定

### 7.1 枚举与状态

| 字段 | 枚举值 | 语义 | 前端展示/动作 | 未知值处理 | 是否允许新增 |
|---|---|---|---|---|---|
| `<status>` | `<active>` | `<语义>` | `<展示或业务动作>` | `<降级/阻断/上报>` | `<是/否及兼容条件>` |

### 7.2 时间、金额、精度和 ID

| 字段 | 传输类型 | 格式/单位 | 时区/精度 | 示例 | 客户端处理 |
|---|---|---|---|---|---|
| `<createdAt>` | `<string>` | `<ISO 8601>` | `<UTC, ms>` | `<示例>` | `<解析与展示规则>` |

### 7.3 缺失、Null 与空值

| 字段 | 缺失语义 | `null` 语义 | 空字符串/空数组语义 | 默认行为 |
|---|---|---|---|---|
| `<field>` | `<语义>` | `<语义>` | `<语义>` | `<服务端/客户端行为>` |

### 7.4 分页、排序与过滤

| 项目 | 契约 |
|---|---|
| 分页模式 | `<page/size、offset/limit 或 cursor>` |
| 首尾页语义 | `<边界行为>` |
| 总数 | `<是否提供、精确或估算>` |
| 默认排序 | `<字段与方向>` |
| 稳定排序键 | `<避免翻页重复/遗漏的规则>` |
| 可用过滤项 | `<字段、操作符、组合规则>` |

## 8. 错误契约

### 8.1 统一错误结构

| 字段 | 类型 | 必填 | 可空 | 说明 |
|---|---|---|---|---|
| `code` | `string` | 是 | 否 | 稳定、机器可读的错误码 |
| `message` | `string` | 是 | 否 | 可展示或可本地化的概述，不作为业务分支依据 |
| `details` | `<object/array>` | 否 | 是 | 字段级或依赖级详情，需定义子结构 |
| `traceId` | `string` | 否 | 是 | 用于日志关联，不包含敏感信息 |

```json
{
  "code": "EXAMPLE_ERROR",
  "message": "Replace with an approved message",
  "details": null,
  "traceId": "trace_example"
}
```

### 8.2 错误码表

| 协议状态 | 错误码 | 触发条件 | 前端动作 | 可重试 | 上报/日志要求 |
|---|---|---|---|---|---|
| `<400>` | `<INVALID_ARGUMENT>` | `<条件>` | `<字段提示或全局提示>` | 否 | `<要求>` |

## 9. 安全与隐私

- 权限与越权边界：`<说明>`
- 敏感字段：`<字段、脱敏、传输和存储要求>`
- 日志限制：`<禁止记录的字段和允许的追踪信息>`
- 输入校验：`<注入、文件、URL、长度、格式等规则>`
- 文件或临时 URL：`<MIME、大小、有效期、访问控制>`
- 审计要求：`<操作人、时间、结果和审计事件>`

## 10. 兼容性与生命周期

| 项目 | 内容 |
|---|---|
| 变更分类 | `非破坏性 / 破坏性` |
| 受影响消费方 | `<前端、服务、SDK、第三方>` |
| 向后兼容窗口 | `<版本或日期范围>` |
| 双轨方案 | `<旧/新接口并存方式>` |
| 迁移步骤 | `<按顺序列出>` |
| 弃用通知日期 | `<YYYY-MM-DD 或无>` |
| 停止服务日期 | `<YYYY-MM-DD 或无>` |
| 回滚条件与方案 | `<触发条件、恢复版本、数据处理>` |

## 11. QA 门禁与证据

| 门禁 | 结果 | 证据链接 | 负责人 | 日期 |
|---|---|---|---|---|
| 字段完整性与示例有效 | `pending/pass/fail` | `<链接>` | `<QA>` | `<YYYY-MM-DD>` |
| Schema/前端类型/后端模型一致 | `pending/pass/fail` | `<链接>` | `<QA/Dev>` | `<YYYY-MM-DD>` |
| 正常路径 | `pending/pass/fail` | `<链接>` | `<QA>` | `<YYYY-MM-DD>` |
| Null、空值、边界、精度、时区 | `pending/pass/fail` | `<链接>` | `<QA>` | `<YYYY-MM-DD>` |
| 未知枚举与兼容降级 | `pending/pass/fail` | `<链接>` | `<QA/Frontend>` | `<YYYY-MM-DD>` |
| 错误码、重试和追踪信息 | `pending/pass/fail` | `<链接>` | `<QA>` | `<YYYY-MM-DD>` |
| 鉴权、越权、敏感数据和输入校验 | `pending/pass/fail` | `<链接>` | `<QA/Security>` | `<YYYY-MM-DD>` |
| 旧新版本兼容或迁移阻断 | `pending/pass/fail` | `<链接>` | `<QA>` | `<YYYY-MM-DD>` |

未通过项与例外：`<原因、风险、批准人、到期日和补救任务>`

## 12. 评审与批准

| 角色 | 确认人 | 结论 | 日期 | 备注 |
|---|---|---|---|---|
| 接口 Owner | `<name>` | `pending/approved/changes-requested` | `<date>` | `<备注>` |
| 前端消费方 | `<name>` | `pending/approved/changes-requested` | `<date>` | `<映射/兼容结论>` |
| QA | `<name>` | `pending/approved/changes-requested` | `<date>` | `<门禁结论>` |
| Security-Reviewer | `<name>` | `pending/approved/changes-requested` | `<date>` | `<安全结论>` |
| Doc | `<name>` | `pending/approved/changes-requested` | `<date>` | `<索引与链接结论>` |

## 13. Doc 更新记录

- [index](./index.md)：`<已更新 / 无需更新 / 待确认 / blocked>`
- [architecture](../../project/architecture.md)：`<已更新 / 无需更新 / 待确认 / blocked>`
- [verification](../../project/verification.md)：`<已更新 / 无需更新 / 待确认 / blocked>`
- Schema、SDK、Mock 和其他相关文档：`<链接与结论>`
- 历史版本、弃用和替代链接：`<链接与结论>`

## 14. 变更记录

| 日期 | 版本 | 变更摘要 | 兼容性 | 提出人 | 批准人 |
|---|---|---|---|---|---|
| `<YYYY-MM-DD>` | `<version>` | `<摘要>` | `<非破坏性/破坏性>` | `<name>` | `<name>` |
